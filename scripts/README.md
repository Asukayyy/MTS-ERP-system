# scripts 目录

存放开发辅助脚本。**当前为空占位**，框架阶段不需要脚本。

后续可以放的内容（按需，不要提前引入）：

- 一键启动前后端的脚本
- 初始化本地数据库 / 导入基础数据的脚本
- 批量健康检查脚本

约定：

1. 脚本必须是可跨平台使用或明确标注适用平台（Windows `.ps1` / macOS·Linux `.sh`）。
2. 脚本里**不允许写死数据库密码**，需要配置时从 `backend/.env` 读取。
3. 脚本的运行产物（日志、导出文件）不要提交到仓库。
